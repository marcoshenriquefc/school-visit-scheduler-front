# Layouts

## `default`
Used for generic/public pages like login.

## `admin`
Used by admin routes with sidebar, header and user menu.

## `public-form`
Used by `/f/[slug]`, centered card layout, no admin sidebar.
